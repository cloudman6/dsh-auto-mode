{
  "name": "dsh-auto-mode",
  "version": "0.0.0-experimental",
  "private": true,
  "type": "module",
  "scripts": {
    "aa:snapshot": "node scripts/aa-snapshot-refresh.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22"
  }
}
