# AGENTS.md

## 当前阶段

本项目处于规范评审阶段。`docs/spec.md` 是产品范围和验收标准的主文档，`docs/decisions/` 保存不可由代码本身解释的重要决策。

在用户确认规范之前：

- 可以修订、审查和补充设计文档。
- 不创建产品代码、依赖、构建配置、CI 或发布流程。
- 不把 `Proposed` ADR 当成已经接受的实现约束。

## 文档规则

- 默认使用中文；公共发布需要英文时另行设计同步策略。
- 区分事实、决策、假设和开放问题，不把推测写成现状。
- 一个设计事实只保留一个权威位置，其他文档通过链接引用。
- 修改关键架构决策时，新建或更新 ADR 状态；不要删除历史 ADR。
- 每个 Markdown 文件以一个换行结束。

## 产品原则

- 质量基线优先：以固定 `strong` route 为基线，先优化延迟，再优化成本。
- 不把用户或父 Agent 的模型选择当作正确标签。
- 自动决策必须可解释、可审计、可恢复。
- Host 策略拥有常规决策权；模型只能提供任务意图或可选语义评估。
- 高风险、分布外或证据不足的任务必须 `abstain`，使用安全 fallback。

## 安全边界

- 不提交密钥、token、`.env` 或账号信息。
- 不用裸 Git 回滚实现工作区恢复。
- 不允许父 Agent 默认绕过 Routing Policy 指定任意 provider/model。
- 任何进入模型上下文的恢复指令都必须通过可持久化通道记录。
